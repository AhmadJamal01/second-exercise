import { ColorBarDirective } from './color-bar.directive';

describe('ColorBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBarDirective();
    expect(directive).toBeTruthy();
  });
});
